@using VendorAndOrderTracker.Models;

<h1>Vendor Page</h1>

@if (Model.Count == 0)
{
<p> No Vendors are in the list></p>
}
<ul>
@foreach (Vendor vendor in Model)
{
<li><a href="/vendors/@vendor.Id">@vendor.VendorName</a></li>
}

</ul>
<a href="vendors/new"> Add new vendor</a>